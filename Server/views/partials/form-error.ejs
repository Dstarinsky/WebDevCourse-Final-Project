<%# Locals: error (string or null) %>
<% if (error) { %>
    <div class="form-alert mb-4" role="alert">
        <i class="bi bi-exclamation-circle" aria-hidden="true"></i>
        <span><%= error %></span>
    </div>
<% } %>
